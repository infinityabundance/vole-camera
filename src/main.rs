//! VOLE-Camera command-line interface.
//!
//! The first complete deliverable is `vole-camera demo` (§13): a deterministic
//! simulated sensor pipeline that runs on an ordinary Linux desktop without a
//! camera. Subsequent commands (`simulate`, `inspect`, `verify`, `play`,
//! `export-raw`, `court …`) land across Phases C–H.

fn main() {
    // Placeholder until the CLI subcommands are implemented in Phases B–H.
    // Kept minimal so the crate compiles and lints cleanly from Phase A.
    eprintln!("vole-camera: implementation in progress (Phase A/B)");
    std::process::exit(1);
}
